function is_self_net() { 
  #if net.type = NET_IP4 then {  
  #  return net ~ OWNNETSET;
  #}

  if net.type = NET_IP6 then {
    return net ~ OWNNETSETv6; 
  }
  return false;
}

function remove_community() {
  bgp_large_community.delete([(OWNAS, 1, *), (OWNAS, 2, *)]);
}
